<!DOCTYPE html>
<html>

<head>
	<% if(title) { %>
		<title>
			<%= title %>
		</title>
		<% } else { %>
			<title>Chase Sunset</title>
			<% } %>
				<meta name="viewport" content="user-scalable=no, width=device-width, initial-scale=1, maximum-scale=1">
</head>

<body>
	<div id="content"></div>
	<script src="/bundle.js"></script>
</body>

</html>
